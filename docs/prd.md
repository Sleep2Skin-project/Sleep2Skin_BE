# sleep2skin PRD (제품 요구사항 정의서)

> **출처**: Notion 「기능명세서」(UI/사용자 흐름 중심 명세서) — 멋사 14기 중앙해커톤
> **작성 기준일**: 2026-08-04 · 명세서 최종 수정 2026-08-01
> **대상 리포지토리**: `sleep2skin_be` (Spring Boot 백엔드)

---

## 1. 제품 개요

### 한 줄 정의

애플워치가 기록한 **수면 데이터를 '피부 언어'로 번역**해, 오늘의 피부 상태를 예보하고 셀피로 검증하며, 검증 결과로 개인 모델을 학습시키는 모바일 서비스.

### 핵심 가치 제안

기존 피부 앱은 "지금 피부가 어떤가"를 보여주지만, sleep2skin은 **"어젯밤 수면 때문에 오늘 피부가 어떻게 될 것인가"를 미리 알려주고, 그 예보가 맞았는지 사용자가 직접 검증**한다. 검증이 쌓일수록 예보가 개인화된다.

### 핵심 루프

```
① 수면 수집        앱이 HealthKit에서 읽어 서버로 전달 (앱 시작 시)
      ↓
② 피부 예보        수면 지표 × 개인 가중치 → 피부 지표 3종 0~100 점수
      ↓
③ 행동 처방        예보 기반으로 "오늘 할 것 / 피할 것" 추천 (TODO)
      ↓
④ 셀피 검증        5초 셀피 → LLM Vision 실측 → 예보 vs 실측 대조 → 적중률
      ↓
⑤ 개인 모델 학습    오차 방향·크기로 개인 가중치 보정 → ②의 정확도 상승
      ↓
⑥ 리포트           일간·주간·월간·종합으로 누적 패턴 제시, 필요 시 클리닉 연결
```

이 루프의 **②③④⑤가 전부 서버 로직**이며, 이 백엔드 프로젝트의 실질적 범위다.

### 피부 지표 3종 (확정)

예보(HOME-03)와 실측(HOME-06)은 HOME-07에서 1:1 대조되므로 **반드시 동일한 세트**를 사용한다.

| UI 표시명 | enum | 100점에 가까울수록 | 주 영향 수면 요인 ⚠️임시 |
|---|---|---|---|
| **다크서클 회복** | `DARK_CIRCLE` | 눈 밑이 **맑다** (어두움이 적다) | 야간 각성 횟수, 총 수면 시간 |
| **혈색** | `COMPLEXION` | 안색에 **생기가 돈다** | HRV·안정시 심박, 취침 규칙성 |
| **장벽** | `BARRIER` | 피부 장벽이 **튼튼하다** | 깊은 수면·REM 시간 |

모든 지표는 **0~100 점수**로 정규화하며, **점수가 높을수록 좋은 상태다.** 세 지표 모두 예외 없이 같은 방향이다.

**`DARK_CIRCLE`은 "다크서클이 심한 정도"가 아니라 "회복된 정도"다.** 이름만 보면 반대로 읽히기 쉬워 UI 표시명을 **"다크서클 회복"**으로 둔다. 각성이 많은 밤일수록 이 점수는 **내려간다**.

> **이 방향은 예보와 실측 양쪽에 똑같이 적용된다.** 한쪽만 뒤집히면 HOME-07 대조가 통째로 무의미해지고, 개인 가중치가 반대 방향으로 학습된다. LLM Vision 프롬프트에도 명시한다 → [architecture.md](architecture.md) §OpenAI Vision

> ⚠️ **지표 3종은 확정이지만, "주 영향 수면 요인" 매핑은 임시값이다.** 팀 논의로 재확정 예정. §9 참조.

---

## 2. 사용자 및 범위

| 항목 | 내용 |
|---|---|
| 주 사용자 | 애플워치로 수면을 기록하는 20~30대, 피부 컨디션 기복을 겪는 사람 |
| 클라이언트 | iOS 모바일 앱 (HealthKit·카메라·로컬 푸시 알림 의존) |
| 백엔드 역할 | REST API 제공. 수면 정규화, 예보 산출, 셀피 분석 중계, 개인 모델 학습, 추천 엔진, 리포트 집계 |
| 프로젝트 성격 | 해커톤 데모 (멋사 14기 중앙해커톤) — 재현성과 구현 속도를 정확도보다 우선 |
| 제외 범위 | 의료 진단. 트리아지(REP-09) 결과에는 면책 문구 고정 노출: *"의료 진단이 아닙니다. 기록 패턴에 기반한 참고 신호입니다."* |

### 확정된 기술 결정

| 항목 | 결정 | 비고 |
|---|---|---|
| 피부 지표 | 3종 — `DARK_CIRCLE` · `COMPLEXION` · `BARRIER` | 예보/실측 동일 세트 |
| 인증 | **없음** — 테스트 유저를 DB에 직접 주입 | 해커톤 범위. 로그인 화면 없음 |
| DB | MySQL + Spring Data JPA. 운영은 **AWS RDS(MySQL)** | 앱 컨테이너와 분리 — 배포해도 데이터가 남는다 |
| LLM Vision | OpenAI `gpt-5.6-terra` (Responses API + Structured Outputs) | 프로퍼티로 모델 교체 가능 |
| 이미지 저장 | **하지 않는다** — 멀티파트 수신 → 메모리에서 LLM 전달 → 지표 숫자만 RDS 저장 | 오브젝트 스토리지(S3) 미사용. §5.2 참조 |
| HealthKit 수집 | **앱이 읽어 서버로 전달** (서버 배치 아님) | 앱 시작 시 업로드 → 즉시 스코어링 |
| 배포 | AWS EC2 + Docker + RDS + CI/CD 파이프라인 | |

### 셀피 이미지 취급 정책 (제품 정체성)

셀피는 **분석 직후 즉시 삭제**하며, 얼굴을 복원할 수 있는 어떤 데이터도 보관하지 않는다. 지표 3종의 숫자만 RDS에 남는다. 이 정책은 온보딩(ONB-04)에서 고지되고 마이페이지(MY-04)에서 실제 실행 지점을 제공하는, **마케팅 문구가 아니라 구현 제약**이다.

> ⚠️ **정확한 표현 주의**: 이미지를 파일로 어디에도 쓰지 않지만, 처리 중 서버 메모리를 거치고 OpenAI로 전송된다. 따라서 "저장하지 않습니다"가 아니라 **"분석 직후 즉시 삭제하며, 얼굴을 복원할 수 있는 데이터를 보관하지 않습니다"**로 표현해야 한다. 이를 뒷받침하는 구현은 §5.2 참조.

---

## 3. 정보 구조

```
[HOME] [TODO] [REPORT] [MY]          ← 하단 탭 네비게이션
※ 온보딩(ONB-01~05)은 탭 밖 진입 플로우
```

### 온보딩 플로우

```
시작 화면(ONB-01)
   ↓
개인정보 동의(ONB-02) ──no──→ 앱 종료
   │ yes
   ↓
수면 데이터 연결(ONB-03) ──yes/no 모두──→ 랜딩 HOME(ONB-05)
```

> 미연결 사용자도 HOME에 진입하므로 **HOME-02·HOME-03의 빈 상태 처리가 필수**다. (§7 참조)

### 화면 도메인 ↔ 백엔드 패키지 매핑

화면 도메인과 백엔드 데이터 도메인은 1:1이 아니다. HOME 화면 하나가 수면·피부 두 데이터 도메인을 걸친다.

| 화면 도메인 | 백엔드 패키지 | 근거 |
|---|---|---|
| ONBOARDING | `user` | 동의 이력, 온보딩 완료 상태 |
| HOME | `sleep` + `skin` | 수면 통역(HOME-02)은 sleep, 예보·검증·학습(HOME-03/06/07/08)은 skin |
| TODO | `todo` | 추천 엔진, 체크리스트 |
| REPORT | `report` | 집계·분석 전용 (sleep·skin 조회) |
| MY | `user` | 프로필, 데이터 관리 |

상세는 [architecture.md](architecture.md) 참조.

---

## 4. 기능 명세

우선순위: `high` = MVP 필수 / `medium` = 데모 품질 향상 / `low` = 후순위
난이도: 명세서의 개발 난이도(상·중·하)
BE: 서버 작업 유무 — **`—` 표시는 클라이언트 전용, 백엔드 구현 대상 아님**

### 4.1 온보딩 (ONB)

| ID | 기능명 | 우선 | 난이도 | 백엔드 처리 |
|---|---|---|---|---|
| ONB-01 | 앱 시작 화면 (가치 제안) | high | 하 | — (로컬 온보딩 완료 플래그만 확인) |
| ONB-02 | 개인정보 수집·이용 동의 (분기) | high | 하 | 동의 이력 저장(약관 버전·동의 시각·사용자 식별자) |
| ONB-03 | 수면 데이터 연결 (HealthKit) | high | 중 | **앱이 전달한 수면 세션 수신 → 정규화 → DB 저장** |
| ONB-04 | 프라이버시 고지 (셀피 미저장) | high | 하 | — (정적 화면) |
| ONB-05 | 랜딩 진입 및 온보딩 완료 처리 | high | 하 | 온보딩 완료 상태 및 초기 설정 저장 |

> **ONB-03 난이도 하향** (상→중): 서버가 HealthKit에 직접 접근하지 않고 앱이 읽어 전달하는 구조로 확정되어, 서버는 수신·정규화·저장만 담당한다.

**수집 데이터 5종** (앱이 HealthKit에서 읽기 전용으로 취득 후 전달):

| 수면 데이터 | 피부 측면의 의미 | 연결 지표 ⚠️임시 |
|---|---|---|
| ① 깊은 수면·REM·코어 (분) | 장벽 재생 / 콜라겐 지표 | `BARRIER` |
| ② **잠든** 시각 · 기상 시각 | 수면 규칙성 | `COMPLEXION` |
| ③ 야간 각성 횟수 | **다크서클 예측의 핵심 변수** | `DARK_CIRCLE` |
| ④ HRV · 안정시 심박 | 스트레스성 트러블 신호 | `COMPLEXION` |
| ⑤ 각성 총 시간 (분) | 각성의 길이 — 횟수만으로는 안 잡히는 축 | 미정 (§9.1) |

**③⑤의 각성 판정 기준 — 5분** (2026-08-06 확정, §7 B5). **5분 이상 지속된 `AWAKE` 구간만** 각성 1회로 세고, 총 시간도 **그 구간들만** 합산한다. 5분 미만은 뒤척임으로 보고 버린다. 임계값이 없으면 30초 뒤척임까지 세어 각성 횟수가 사람마다 20회씩 나온다.

- **③과 ⑤에 같은 임계값을 쓴다.** REP-04가 둘을 나란히 보여주므로, 횟수에만 걸면 "각성 0회 · 총 12분" 같은 모순이 화면에 나온다
- **마지막 `asleep` 이후의 `AWAKE`는 제외한다.** 그건 야간 각성이 아니라 기상이다. 세면 모든 사용자가 매일 최소 1회를 깔고 시작해 다크서클 예보가 전반적으로 눌린다
- **서버가 단계 구간에서 계산한다.** 앱이 보낸 각성 횟수를 그대로 받지 않는다 — 임계값이 서버 상수여야 앱 배포 없이 바꿀 수 있고 전 사용자에게 같은 기준이 적용된다. → [erd.md](erd.md) §3.3

> 수집 데이터 5종은 확정이나 **연결 지표 매핑은 임시값**이다. §9 참조. **⑤는 2026-08-06 추가.**

**수집 세트와 표시 세트는 다르다.** 사용자에게 보여주는 것은 REP-04의 **4종**이고, 나머지는 화면에 나오지 않지만 예보 산출에 쓰인다.

**수집 · 피처 · 표시는 서로 다른 세 층위다.** "저장한다"가 "스코어링에 쓴다"를 뜻하지 않는다.

| 항목 | 수집·저장 | **스코어링 피처** | 표시 (REP-04) |
|---|:---:|:---:|:---:|
| 총 수면 시간 | ✓ | `TOTAL_SLEEP` | ✓ |
| 깊은 수면 | ✓ | `DEEP_SLEEP` | ✓ |
| REM | ✓ | `REM_SLEEP` | — |
| 코어 | ✓ | — | — |
| 각성 횟수 | ✓ | `AWAKE_COUNT` | ✓ |
| 각성 총 시간 | ✓ | 배정 미정 (§9.1) | ✓ |
| 잠든 시각 | ✓ | `BEDTIME_REGULARITY`로 파생 | — |
| 기상 시각 | ✓ | — | — |
| HRV | ✓ | `HRV` | — |
| 안정시 심박 | ✓ | `RESTING_HEART_RATE` | — |

**수집 10항목 · 피처 7종 · 표시 4종.** 피처 목록의 확정 근거는 §9.1에 있다.

**피처가 아닌 것도 버리지 않는다.** 코어는 `asleepUnspecified` 때문에 단계 합이 총 수면과 안 맞는 경우를 확인하는 데 쓰이고([erd.md](erd.md) §3.3), 기상 시각은 총 수면 시간 검증과 화면 표현에 쓰인다. 나중에 피처로 승격할 여지도 남는다.

**②의 기준은 "잠든 시각"(첫 `asleep` 시작)이지 "누운 시각"이 아니다.** 누운 시각은 HealthKit `inBed` 샘플에서만 나오는데 **안 남는 밤이 생긴다.** 두 기준을 섞어 규칙성을 재면 규칙적으로 자는 사용자도 편차가 크게 나와 혈색 예보만 나빠진다.

**그래서 `inBed`에 의존하는 지표는 수집·표시 양쪽에서 제외했다** — 누운 시각 · 누운 시간 · **입면 지연** · **수면 효율**. 넷 다 함께 결측되므로 피처로 쓰면 스코어링에 결측 분기가 생긴다. → [erd.md](erd.md) §3.3

### 4.2 HOME — 예보와 검증

| ID | 기능명 | 우선 | 난이도 | 백엔드 처리 |
|---|---|---|---|---|
| HOME-01 | 날짜 및 웰컴 멘트 | high | 하 | — (클라이언트 로컬 시각) |
| HOME-02 | 어젯밤 수면 통역 카드 | high | 중 | 수면 세션 정규화 → 기준치 대비 편차 → 가장 부족한 지표 1개 선정 → 헤드라인 템플릿 주입 |
| HOME-03 | **오늘의 피부 예보** | high | **상** | 수면 지표 가중합 스코어링 → 개인 가중치 반영 → 0~100 정규화 → 등급 컷오프(§10.1) → 오늘자 예보 저장 |
| HOME-04 | 게이미피케이션 (캐릭터·레벨·EXP) | low | 중 | **방향 미확정** — EXP 적립 트리거·레벨 테이블 미정 |
| HOME-05 | 셀피 검증 — 촬영 | high | 중 | — (촬영 단계 서버 전송 없음, 갤러리 저장 안 함) |
| HOME-06 | 셀피 검증 — 분석 (LLM Vision) | high | **상** | 멀티파트 수신 → 메모리에서 OpenAI Vision 분석 → 0~100 정규화 → 지표만 저장. **원본은 어디에도 쓰지 않는다** |
| HOME-07 | 셀피 검증 — 결과 (예보 vs 실측) | high | 중 | 지표별 `예보−실측` → 판정 매핑(§10.2) → 적중률 → 이력·연속일수 갱신 → HOME-08 학습 트리거 |
| HOME-08 | 개인 모델 학습 (가중치 보정) | medium | **상** | 오차 방향·크기 반영해 개인 가중치 업데이트(단순 평균 이동) → 다음 예보에 즉시 적용 → 변화 설명 문장 생성 |
| HOME-09 | 예보 적중률 · 연속 검증 배너 | medium | 하 | 최근 검증 1건 조회 + 연속일수 계산 |

**HOME-03 → HOME-07 연결**: HOME-03이 저장한 오늘자 예보값이 HOME-07 대조의 기준이 된다. 예보 산출 없이는 검증이 성립하지 않는다.

**셀피 검증 서브플로우**: `HOME-05 촬영 → HOME-06 분석(자동 전환) → HOME-07 결과 → "오늘의 스킨 처방" → TODO 탭`

### 4.3 TODO — 행동 처방

| ID | 기능명 | 우선 | 난이도 | 백엔드 처리 |
|---|---|---|---|---|
| TODO-01 | 날짜 및 요약 멘트 | high | 중 | 가장 취약한 지표 기준 처방 테마 선정 → 테마별 멘트 템플릿 매핑 |
| TODO-02 | **리포트 기반 행동 추천 (추천 엔진)** | high | **상** | 지표별 임계값 룰 매칭 → 액션 후보 추출 → 영향도순 정렬 → 구분별 상위 3개 |
| TODO-03 | '오늘은 피하세요' 리스트 | high | 하 | TODO-02 결과 필터링·정렬 |
| TODO-04 | '오늘은 이렇게' 리스트 | high | 하 | TODO-02 결과 필터링 + 완료 상태 조인 |
| TODO-05 | TODO 리스트 체크·완료 처리 | high | 중 | 체크 상태 저장 → 일자별 달성률 집계 → REP-10 집계 반영 |
| TODO-06 | TODO 추가항목 선택 | medium | 중 | 사용자 커스텀 항목 저장 및 당일 목록 병합 |
| TODO-07 | 저녁 수면 가이드 (취침 목표·시나리오 비교) | high | **상** | 취침 시각별 깊은수면 확보량 시뮬레이션 → 다음날 장벽 지수 추정 → 취침 알림 예약 시각 반환 |

**TODO-02는 TODO 탭 전체의 원천 로직**이다. 결과를 `피하세요`(TODO-03) / `이렇게`(TODO-04)로 분기한다.

**액션 마스터 데이터**가 필요하다 — 항목명·근거 문구·매칭 조건. 명세서 기준 **개발보다 콘텐츠 작업량이 큰 최대 작업 항목**.

예시 항목:
- 피하세요: 강한 각질 제거 / 뜨거운 물 세안 / 새 제품 첫 사용
- 이렇게: 수분 세럼 2회 레이어링 / 크림으로 장벽 봉인 / SPF50 재도포

**TODO-07 예시 출력**: `23:40 취침 → 장벽 78·회복` vs `02:00 취침 → 장벽 45·악화`, 그리고 권장 취침 시각.

> 초안에 있던 **밤 체크리스트(3항목 고정 노출 + 상태 3종)는 제외됐다.** TODO 항목은 액션 마스터의 `AVOID`/`DO` 2종뿐이고, 상태는 `PENDING`/`DONE` 2종뿐이다. TODO-07은 **시뮬레이션과 취침 시각 계산만** 담당한다.

### 4.4 REPORT — 누적 분석

| ID | 기능명 | 우선 | 난이도 | 백엔드 처리 |
|---|---|---|---|---|
| REP-01 | 리포트 탭 네비게이션 (일간·주간·월간·종합) | high | 하 | — (클라이언트 라우팅) |
| REP-02 | 일간 — 인사이트 및 오늘의 액션 | high | 중 | 지표 조회 → 목표 달성 판정 → 인사이트 문장 → TODO-02 상위 2건 인용 |
| REP-03 | 일간 — 수면 단계 타임라인 | medium | 중 | 수면 구간 병합·정렬 → 단계별 누적 시간 집계 → 렌더용 배열 |
| REP-04 | 일간 — 수면 관련 수치 (요약 통계) | high | 하 | 수면 세션 집계·포맷팅 **(표시 4종: 총 수면 / 깊은수면 / 각성 횟수 / 각성 총 시간)** |
| REP-05 | 일간 — 오늘의 피부 지표 (전일 대비) | high | 하 | 전일 값 조회 → 차이 계산 → 부호 포함 포맷팅 (지표 3종) |
| REP-06 | 주간 — 수면 점수 추이 | high | 중 | 7일치 집계 → 조건부 평균 비교로 인사이트 생성 → 적중률·검증일수 집계 |
| REP-07 | 주간 — 피부 직결 원인 및 영향도 (상관 강도) | medium | **상** | 피처·지표 간 상관계수 → 구간별 라벨 매핑 → 영향도순 정렬 |
| REP-08 | 월간 — 주차별 수면 점수표 | medium | 중 | 월단위 주차 분할 → 주차별 평균 → 전월 대비 증감 → 인사이트 |
| REP-09 | 종합 — 종합 판단 (트리아지) | medium | **상** | 수면 목표 달성 & 특정 피부 지표만 N주간 변화 없음 → 트리아지 조건 판정 → 근거 문장 생성 |
| REP-10 | 종합 — 앱이 도와준 / 클리닉 필요 항목 | medium | 중 | 지표별 개선 여부로 영역 분류 |
| REP-11 | 엠레드 클리닉 상담 연결 | low | 하 | 연결 클릭 이벤트 기록(제휴 지표용) — 필요 시 |
| REP-12 | 내 모델 — 일반 vs 개인화 | medium | 중 | 개인 vs 일반 가중치 비율 → 최대 편차 피처로 발견 문장 → 검증 횟수 기반 신뢰도 |

**REP-07 상관 쌍** ⚠️ **임시값** — 지표 3종 확정에 맞춰 임시 재정의한 것이다. 원 명세의 "총 수면시간→유분"이 유분 지표 제외로 무효가 되어 자리를 메운 상태이며, **팀 논의로 재확정 예정**이다. §9 참조.

| 수면 피처 | → 피부 지표 | 비고 |
|---|---|---|
| 야간 각성 횟수 | `DARK_CIRCLE` | 원 명세 유지 |
| 총 수면 시간 | `DARK_CIRCLE` | **임시** — 원래 유분에 연결돼 있었음 |
| 깊은 수면 시간 | `BARRIER` | 원 명세 유지 |
| REM 수면 시간 | `BARRIER` | **임시** — 2026-08-06 승격 |
| 취침 규칙성 | `COMPLEXION` | **임시** — 원 명세의 "톤"을 혈색으로 대체 |
| HRV | `COMPLEXION` | **임시** — 원 명세에 쌍 없음. 신규 추가 |
| 안정시 심박 | `COMPLEXION` | **임시** — 2026-08-06 승격 (HRV와 분리) |

> §9.1과 **같은 7쌍**이다. 두 표가 어긋나면 REP-07이 예보와 다른 근거를 말하게 된다 — 매핑을 바꿀 땐 §9.1을 고치고 이 표를 맞춘다.

**상관 강도 라벨 4단계**: 매우 강함 / 강함 / 보통 / 약함

**REP-09 트리아지 조건**: 수면 목표는 달성했는데 특정 피부 지표만 N주간 정체 → "수면으로는 잡히지 않는 신호"로 판별.

**REP-10 영역 구분**:
- 앱 담당: 피부 장벽·푸석함 / 다크서클 / 일시적 염증
- 클리닉 필요: 색소침착 / 여드름 흉터 / 구조적 노화

### 4.5 MY — 설정과 데이터 관리

| ID | 기능명 | 우선 | 난이도 | 백엔드 처리 |
|---|---|---|---|---|
| MY-01 | 프로필 및 모델 신뢰도 요약 | medium | 하 | 누적 검증 일수 → 신뢰도 등급(下/中/上) 매핑 |
| MY-02 | 데이터 연결 상태 (애플 건강 앱) | high | 하 | **마지막 수면 데이터 수신 시각 조회** (서버 배치 없음) |
| MY-03 | 알림 설정 (아침 예보 · 취침 알림) | **low** | 중 | **MVP 제외.** 설정 저장. 실제 알림 예약은 클라이언트 로컬 알림 |
| MY-04 | 프라이버시 및 데이터 관리 | medium | 중 | 내보내기(수면·피부 기록 파일) / **전체 삭제(영구, 복구 불가)** |
| MY-05 | 정보 (버전 · 문의 · 약관) | low | 하 | — (정적 리소스 + 외부 링크) |

> **MY-02 난이도 하향** (중→하): 서버 배치 동기화가 사라져 "마지막 수신 시각 조회"만 남는다. 동기화 주기 표기는 앱의 업로드 정책(앱 시작 시)을 그대로 노출한다.

**MY-04 저장 정책 표시값**: 셀피 원본 저장 = `저장 안 함(고정)` — 설정 토글이 아니라 **정책 표시**다.

---

## 5. 외부 연동

| 연동 대상 | 용도 | 관련 기능 | 비고 |
|---|---|---|---|
| Apple HealthKit | 수면 세션 읽기 | ONB-03, MY-02, REP-03 | **앱이 직접 읽어 서버로 전달.** 서버는 HealthKit에 접근하지 않음 |
| OpenAI Vision (`gpt-5.6-terra`) | 셀피 → 피부 지표 실측값 산출 | HOME-06 | Responses API + Structured Outputs로 스키마 강제. 이미지는 요청 본문에 base64 인라인 |
| AWS RDS (MySQL) | 모든 영속 데이터 | 전체 | EC2 앱 컨테이너와 분리. 셀피는 지표 숫자만 들어간다 |
| 로컬 푸시 알림 | 아침 예보 · 취침 알림 | TODO-07, MY-03 | 클라이언트 로컬 알림 (서버 푸시 아님) |
| 엠레드 클리닉 웹 | 트리아지 후 상담 연결 | REP-11 | 해커톤 범위에서는 단순 외부 링크 |

### 5.1 수면 데이터 수신 (HealthKit)

서버는 HealthKit에 직접 접근할 수 없다. 앱이 권한을 얻어 읽은 데이터를 서버로 전달한다.

```
[앱 시작]
  → 앱이 HealthKit에서 지난밤 수면 세션 읽기
  → POST /api/v1/sleep/sessions  (수면 세션 페이로드)
  → 서버: [중복 판단] → 정규화 → 저장 → 즉시 스코어링(HOME-03) → 예보 응답
  → 앱: HOME 화면에 예보 표시
```

이 구조의 결과:
- **서버 스케줄러가 필요 없다.** 배치 인프라와 실패 재시도 설계가 통째로 사라진다.
- HealthKit 단계 구분이 기기·OS 버전별로 다를 수 있으므로 **앱↔서버 간 수면 단계 매핑 규칙을 계약으로 고정**해야 한다.

#### 중복 수신 차단 (필수)

앱이 시작될 때마다 업로드하므로, **새 수면 데이터가 생기기 전까지는 같은 세션이 반복 전송된다.** 하루에 앱을 다섯 번 켜면 같은 데이터가 다섯 번 온다.

이때 **처리를 시작하기 전에 중복을 판단해 되돌린다.** 저장 후 덮어쓰는 upsert가 아니라, **아예 처리하지 않는다.**

```
POST /api/v1/sleep/sessions 수신
  │
  ├─ 페이로드 정규화 → 내용 해시 계산            ← 저장 전에 수행
  │
  ├─ (user_id, sleep_date)로 기존 세션 조회
  │
  ├─ ① 기존 없음        → 저장 + 스코어링 + 예보 산출    → 201 Created
  ├─ ② 기존 있음·해시 동일 → 아무것도 하지 않음
  │                       → 기존 예보를 그대로 반환      → 200 OK
  └─ ③ 기존 있음·해시 다름 → 그날 검증을 마쳤는가?
                             ├ 검증 완료 → 갱신하지 않음. 기존 예보 반환 (processed=false)
                             └ 검증 전   → 갱신 + 재산출        (processed=true)
```

**②에서 재처리를 막아야 하는 이유** — 단순한 성능 문제가 아니다:

| 이유 | 설명 |
|---|---|
| **검증 무효화 방지** | 예보를 다시 산출하면 오늘자 예보값이 바뀐다. 이미 셀피 검증(HOME-07)을 마쳤다면 **대조 기준이 사후에 바뀌어 적중률이 조작된 셈**이 된다 |
| 학습 오염 방지 | 재산출이 개인 가중치 학습(HOME-08)을 다시 트리거하면 같은 데이터로 여러 번 학습해 가중치가 왜곡된다 |
| 비용 | 스코어링·집계가 무의미하게 반복된다 |

**응답에 처리 여부를 명시한다.** 앱이 "새 예보인지 기존 예보인지"를 알아야 화면 갱신 여부를 판단할 수 있다.

```json
{ "success": true, "data": { "processed": false, "forecast": { ... } }, "error": null }
```

**구현 요건**:
- `SleepSession`에 `payload_hash` 컬럼(정규화된 페이로드의 SHA-256)을 둔다
- `(user_id, sleep_date)`에 유니크 제약을 걸어 DB 레벨에서도 중복을 막는다
- 해시 계산은 **정규화 후** 수행한다. 앱이 필드 순서를 바꿔 보내도 같은 데이터로 판정되어야 한다

### 5.2 셀피 이미지 파이프라인 (스토리지 없음)

"얼굴 데이터를 보관하지 않는다"를 **설명이 아니라 구조로 증명**하는 것이 목표다. 그래서 **이미지를 저장할 곳 자체를 두지 않는다.**

```
[앱] 셀피 촬영 (갤러리 저장 안 함)
  → POST /api/v1/skin/selfie  (multipart/form-data)
  → 서버: 요청 바이트를 메모리에서 그대로 OpenAI Vision에 전달
           (디스크·버킷에 쓰지 않는다)
  → 지표 3종 0~100 산출
  → RDS에 숫자 지표 3개만 저장
  → 앱에 결과 반환 → 응답이 끝나면 바이트는 참조가 끊긴다
```

**보관하지 않음을 뒷받침하는 구현 장치**:

| # | 장치 | 목적 |
|---|---|---|
| 1 | **오브젝트 스토리지를 쓰지 않음** | 업로드할 버킷이 없으면 잔존 사고도 없다 |
| 2 | **DB 스키마에 이미지 경로/URL 컬럼을 두지 않음** | 애초에 이미지를 참조할 방법이 없다. 스키마 자체가 정책의 증거 |
| 3 | **이미지 바이트를 필드·캐시·정적 변수에 담지 않음** | 요청 처리 범위를 벗어나 살아남지 않는다 |
| 4 | **예외 로그에 바이트·base64를 남기지 않음** | 실패 경로로 이미지가 로그에 새는 것을 막는다 |

> **S3 경유 초안에서 바뀐 부분이다.** 임시 업로드를 없애면서 Lifecycle 만료 정책·`finally` 삭제 블록·버킷 접근제어가 **전부 필요 없어졌다.** 지켜야 할 장치가 4개에서 사실상 2개(장치 1·2)로 줄었고, 정책이 깨질 경로도 그만큼 줄었다.

**확인이 필요한 항목**:
- OpenAI API의 데이터 보관 정책 — 요청 로그에 이미지가 남는지, zero data retention 적용 가능한지 확인 후 사용자 고지 문구를 확정해야 한다. **S3를 뺀 뒤에도 이 항목은 그대로 남는다** — 우리 인프라가 아니라 제공자 쪽 보관의 문제다. (§7 B4)
- 이미지가 메모리를 거치는 것은 변하지 않으므로, 고지 문구는 여전히 "저장하지 않습니다"가 아니라 **"분석 직후 즉시 삭제하며, 얼굴을 복원할 수 있는 데이터를 보관하지 않습니다"**여야 한다.

---

## 6. 데이터 모델 초안

| 엔티티 | 핵심 필드 | 생산 기능 | 소비 기능 |
|---|---|---|---|
| 사용자 | 식별자, 닉네임, 온보딩 완료 여부 | ONB-05 | 전체 |
| 동의 이력 | 약관 버전, 동의 시각 | ONB-02 | MY-05 |
| 수면 세션 | 수면 일자(unique), **페이로드 해시**, 단계별 구간, 깊은수면·REM·코어 분, **잠든**·기상 시각, 각성 횟수·총 시간, HRV, 안정시 심박 | ONB-03 | HOME-02/03, REP-03/04/06/08 |
| 피부 예보 | 기준일, 지표 3종 점수(0~100), 등급 | HOME-03 | HOME-07, TODO-01/02, REP-05 |
| 셀피 실측 | 기준일, 지표 3종 점수(0~100), 분석 시각·상태 | HOME-06 | HOME-07 |
| 검증 이력 | 기준일, 지표별 오차·판정, 적중률, 연속일수 | HOME-07 | HOME-08/09, REP-06/12, MY-01 |
| 개인 가중치 | (피처, 지표) 쌍별 가중치 — **사용자당 7행** (§9.1) | HOME-08 | HOME-03, REP-07/12 |
| 액션 마스터 | 항목명, 구분(피하세요/이렇게), 근거 문구, 매칭 조건, 영향도 | (콘텐츠 구축) | TODO-02/03/04/06 |
| TODO 기록 | 기준일, 항목, 완료 상태, 달성률 | TODO-05/06 | REP-10 |
| ~~알림 설정~~ | 알림 종류, 활성 여부, 예약 시각 | MY-03 (**MVP 제외**) | — |

> **원본 셀피 이미지 경로는 어떤 엔티티에도 두지 않는다.** (§5.2 장치 2)

---

## 7. 미결정 사항 (Open Issues)

### ✅ 해결됨

| 이슈 | 결정 |
|---|---|
| ~~피부 지표 체계 불일치~~ | 3종 확정 — `DARK_CIRCLE` · `COMPLEXION` · `BARRIER` (§1) |
| ~~각성 1회의 최소 지속 시간~~ | **5분**. 5분 이상 `AWAKE` 구간만 횟수·총 시간 양쪽에 반영, 마지막 `asleep` 이후 구간(기상)은 제외. 서버가 단계 구간에서 계산 (§4.1) |
| ~~회원·인증 체계~~ | 인증 없음. 테스트 유저 DB 직접 주입 |
| ~~서버 배치 동기화 설계~~ | 앱이 HealthKit 데이터 전달. 서버 스케줄러 없음 (§5.1) |
| ~~S3 접근제어 정책~~ | **S3를 쓰지 않기로 했다.** 멀티파트를 메모리에서 바로 LLM에 넘기고 지표만 RDS에 저장한다 — 버킷이 없으므로 접근제어 이슈 자체가 사라졌다 (§5.2) |
| ~~추천 방식 (룰 vs LLM)~~ | 룰 기반 — 재현성·비용 |
| ~~같은 수면 일자에 내용이 다른 데이터가 온 경우~~ | **검증을 마친 날은 재산출하지 않는다.** 검증 전이면 갱신 후 재산출 (§5.1 케이스 ③) |
| ~~가중치 보정 이력 테이블~~ | **만들지 않는다.** 어떤 화면도 요구하지 않고, 검증 이력에서 재현 가능하다. 디버깅은 로그로 (`log.info` 보정 전후) |
| ~~피부 지표 기준 테이블~~ | **만들지 않는다.** 등급 컷오프·판정 구간은 알고리즘 파라미터이므로 `ScoringPolicy` 코드 상수로 둔다. DB에 두면 스코어링 단위 테스트가 DB에 의존하게 된다 |

### 블로커 — 구현 착수 전 확정 필요

| # | 이슈 | 관련 |
|---|---|---|
| **B1** | **가중합 스코어링 공식** — 수면 피처 → 지표 점수 변환식과 초기(일반) 가중치. **§9.1 임시 매핑을 확정값으로 바꾸는 작업** | HOME-03 |
| ~~B2~~ | ~~등급 컷오프 기준~~ — **확정: 25점씩 4등분** (위험/주의/보통/안정). → §10.1 | (해결) |
| ~~B3~~ | ~~판정 오차 구간~~ — **확정: ±5 적중 / ±6~15 근접 / ±16~ 과소·과대**. → §10.2 | (해결) |
| **B4** | **OpenAI 데이터 보관 정책 확인** — 요청 로그에 이미지 잔존 여부. 사용자 고지 문구 확정에 필요 | HOME-06 |
| **B5** | **수면 단계 매핑 계약** — 앱↔서버 간 HealthKit 단계 값 규격. `asleepUnspecified`를 앱이 `CORE`로 합쳐 보낼지 확정 필요.<br>~~각성 1회의 최소 지속 시간~~ — **확정: 5분** (아래 §4.1 참조) | ONB-03, REP-03/04 |
| **B6** | **수면 목표값** — "깊은 수면 62분 · 목표 달성"의 목표가 몇 분인가. 취침 목표 시각도 함께. **전체 공통 권장치인지 사용자별 목표인지**부터 정해야 한다. 목표를 설정하는 화면이 명세에 없으므로 **공통 권장치 고정을 권장**. 사용자별로 가면 `users`에 컬럼 추가 필요.<br>※ 1단계(예보)에는 영향 없고 **2단계 일간 리포트 착수 전**까지 확정하면 된다 | REP-02/06/09, TODO-07 |
| **B7** | **결측 밤의 스코어링 처리** (2026-08-06 추가) — `HRV`·`RESTING_HEART_RATE`는 NULL이 될 수 있다(워치를 안 차고 잔 밤). **피처 7종 중 2종**이고 **둘이 함께 결측된다.** 그날 `COMPLEXION`은 피처 3개 중 1개(취침 규칙성)만 남는다.<br>선택지: ① 결측 피처 제외 후 남은 피처로 **재정규화** ② 개인 평균/일반 기본값 **대입** ③ 그날 `COMPLEXION` **예보를 내지 않음**(빈 상태 처리)<br>`personal_weight` 학습 처리도 함께 정해야 한다 — 결측 밤의 검증으로 `HRV` 가중치를 갱신하면 안 된다.<br>※ 안정시 심박 승격으로 드러났을 뿐 **`HRV` 하나만 피처였을 때도 이미 있던 공백**이다 | HOME-03, HOME-08 |

### 로직 정합성

| # | 이슈 | 관련 |
|---|---|---|
| L1 | **가중치 산출 소스 통일** — HOME-08 개인 가중치, REP-07 상관 강도, REP-12 비교 수치가 충돌하지 않아야 함 | HOME-08, REP-07, REP-12 |
| L2 | ~~취침 알림 주체 충돌~~ — **MY-03이 MVP에서 제외되어 당분간 무의미하다.** 취침 알림 시각은 TODO-07 계산값이 유일한 출처이고, 앱이 로컬 알림으로 예약한다. MY-03 착수 시 재검토 | (보류) |
| L3 | 통역 멘트 기준 — 일반 권장치 vs 개인 최근 N일 평균 | HOME-02 |
| L4 | REP-02 액션 제안을 TODO-02 엔진 재사용할지 별도 생성할지 | REP-02 |
| L5 | HOME-02 근거 칩과 REP-04 지표가 겹침 — 동일 산출 로직 공유 권장 | HOME-02, REP-04 |
| L6 | 트리아지 발동 임계값 — 몇 주 정체 / 변화폭 몇 점 이하 | REP-09 |
| L7 | 상관 강도 라벨 4단계의 계수 구간. **7일 표본 상관계수는 통계적으로 불안정** — 최소 표본 미달 시 표시 보류 기준 필요 | REP-07 |
| L8 | 신뢰도 등급 3단계 일수 구간 (HOME-08의 30일 기준과 정합) | MY-01 |
| L9 | REP-09 근거 예시의 '볼 부위 색소침착'은 지표 3종에 없음 — 트리아지 근거 문장 재작성 필요 | REP-09 |

### 빈 상태 · 예외 처리

이 서비스는 **빈 상태가 정상 흐름**이다. 조회 API는 200 + 상태 필드로, 동작 API는 4xx + `ErrorCode`로 나눠 처리한다 ([conventions.md](conventions.md) §2 참조).

| # | 이슈 | 관련 |
|---|---|---|
| E1 | **수면 데이터 미전달 사용자의 HOME 빈 상태 정의 + 재유도 동선** | HOME-02/03, MY-02 |
| E2 | 검증 이력 없는 신규 사용자 문구 (예: "첫 검증을 시작해보세요") | HOME-09 |
| E3 | 기록 7일/1개월 미만 사용자 처리 | REP-06, REP-08 |
| E4 | 전일 값 없을 때 증감 표기 | REP-05 |
| E5 | 연속일 리셋 규칙 — 하루 건너뛰면 0으로 초기화 여부 | HOME-09 |
| E6 | ~~TODO 자정 리셋 규칙, 상태 3종의 자동/수동 전환 조건~~ — **해소.** `base_date`가 있어 리셋할 것이 없고([erd.md](erd.md) §3.9), 상태는 사용자가 직접 바꾸는 `PENDING`/`DONE` 2종뿐이라 자동 전환이 없다 | (해결) |
| E7 | 분석 실패·타임아웃 재시도 처리 | HOME-06 |
| E8 | 수면 세션이 여러 개로 쪼개진 경우 병합 기준 | REP-03 |

### 화면 누락 · 순서

| # | 이슈 |
|---|---|
| S1 | **ONB-02 개인정보 동의 화면 없음** — 플로우차트에만 존재 |
| S2 | **TODO-06 추가항목 선택 화면 없음** — 플로우차트에만 존재 |
| S3 | 온보딩 단계 순서 불일치 — 플로우차트(동의→수면연결) vs 와이어프레임 번호(01→02→03) |
| S4 | REP-12 탭 미배정 — REPORT vs MY |
| S5 | HOME-04 게이미피케이션 — 캐릭터 클릭 이후 동작이 '논의 필요'로 비어 있음. HOME-09와 통합 여부 검토 |
| S6 | 탭 명칭 동기화 — 피그마 `[홈][스캔][리포트][설정]` vs 명세 `[HOME][TODO][REPORT][MY]` |

### 정책 · 콘텐츠

| # | 이슈 |
|---|---|
| P1 | **'미동의 = 앱 종료'가 스토어 심사·UX상 적절한지 검토 필요** |
| P2 | '모든 기록 삭제'는 복구 불가 → **2단계 확인 다이얼로그 필수**. 삭제 후 앱 상태(온보딩 복귀 여부) 미정 |
| P3 | 내보내기 파일 형식(JSON/CSV)과 전달 방식 |
| P4 | 약관·개인정보 처리방침 원문 작성 (ONB-02 동의 항목과 버전 일치) |
| P5 | **액션 마스터 데이터 구축 — 콘텐츠 작업량이 최대. 조기 착수 필요** |
| P6 | 서비스명 불일치 — 명세서 REP-10에는 `SkinCast`, 리포·Swagger에는 `sleep2skin` |

---

## 8. 백엔드 구현 우선순위

**핵심 루프를 먼저 세로로 관통**시키는 전략.

### 1단계 — 핵심 루프 관통 (MVP)

1. 공통 기반 — 응답 래퍼, 전역 예외 처리, 에러 코드, DB 연결(로컬 MySQL → 운영 RDS), 테스트 유저 시딩
2. 수면 세션 수신·정규화 (ONB-03) — `POST /api/v1/sleep/sessions`
3. **피부 예보 산출 (HOME-03)** — 서비스의 심장. §7 **B1·B7** 확정 필요 (§9.1 임시 매핑 재확정 포함). 등급 컷오프(B2)는 §10.1로 확정됨
4. 수면 통역 카드 (HOME-02)
5. 셀피 분석 (HOME-06) — 멀티파트 → OpenAI Vision → 지표만 저장 (스토리지 없음)
6. 예보 vs 실측 검증 (HOME-07)

### 2단계 — 처방과 학습

7. 추천 엔진 (TODO-02) + 액션 마스터 데이터 — **콘텐츠 작업 병행 착수**
8. TODO 리스트 (TODO-01/03/04/05)
9. 개인 모델 학습 (HOME-08)
10. 일간 리포트 (REP-02/04/05)

### 3단계 — 리포트 확장

11. 주간 리포트 (REP-06/07)
12. 저녁 수면 가이드 (TODO-07)
13. 적중률 배너 (HOME-09), 프로필 (MY-01), 데이터 연결 상태 (MY-02)

### 4단계 — 후순위

14. 월간(REP-08) · 종합/트리아지(REP-09/10/11) · 내 모델(REP-12)
15. 프라이버시·데이터 관리 (MY-04), TODO-06
16. **알림 설정 (MY-03)** — MVP 제외. `notification_setting` 테이블도 이때 함께 생성한다
16. 게이미피케이션 (HOME-04) — 방향 확정 후

**백엔드 구현 대상 아님** (클라이언트 전용): ONB-01, ONB-04, HOME-01, HOME-05, REP-01, MY-05

---

## 9. 임시값 목록 (재확정 예정)

아래 값들은 **문서를 완성하기 위해 임시로 채워 넣은 것**이며, 팀 논의로 재확정한다. 확정 전까지는 코드에 하드코딩하지 말고 **설정값 또는 상수 클래스 한 곳에 모아** 나중에 한 번에 교체할 수 있게 한다.

### 9.1 수면 피처 → 피부 지표 매핑 ⚠️임시

리포트(REP-07)와 예보 산출(HOME-03) 양쪽에서 쓰인다. **이 매핑이 바뀌면 예보·리포트·학습이 전부 바뀐다.**

**피처 7종** (2026-08-06 확정 — `REM_SLEEP`·`RESTING_HEART_RATE` 승격). 이 표의 각 행이 곧 `personal_weight`의 한 행이다.

| # | 수면 피처 | `sleep_feature` | → 지표 | 근거 상태 | 결측 |
|---|---|---|---|---|---|
| 1 | 야간 각성 횟수 | `AWAKE_COUNT` | `DARK_CIRCLE` | 원 명세 유지 — 비교적 신뢰 | 없음 |
| 2 | 총 수면 시간 | `TOTAL_SLEEP` | `DARK_CIRCLE` | **임시** — 원래 유분에 연결, 유분 제외로 재배치 | 없음 |
| 3 | 깊은 수면 시간 | `DEEP_SLEEP` | `BARRIER` | 원 명세 유지 — 비교적 신뢰 | 없음 |
| 4 | REM 수면 시간 | `REM_SLEEP` | `BARRIER` | **임시** — 승격. 아래 근거 | 없음 |
| 5 | 취침 규칙성 | `BEDTIME_REGULARITY` | `COMPLEXION` | **임시** — 원 명세의 "톤"을 혈색으로 대체. **`sleep_onset_time`(잠든 시각)으로만 계산** | 없음 |
| 6 | HRV | `HRV` | `COMPLEXION` | **임시** — 원 명세에 쌍 없음 | **있음** ⚠️ |
| 7 | 안정시 심박 | `RESTING_HEART_RATE` | `COMPLEXION` | **임시** — 승격. 아래 근거 | **있음** ⚠️ |

지표별 분포: `DARK_CIRCLE` 2 · `COMPLEXION` 3 · `BARRIER` 2

**REM을 승격한 이유는 균형이 아니라 학습 가능성이다.** 피처가 1개뿐인 지표는 개인 가중치가 **점수 전체를 같은 비율로 밀 뿐 아무것도 학습하지 못한다** — 상대 비중을 다툴 상대가 없기 때문이다. REP-07의 `BARRIER` 상관 강도도 같은 이유로 무의미해진다. 단계 값이라 결측도 없어 비용이 거의 없다.

**안정시 심박을 승격한 이유는 그러지 않으면 죽은 데이터이기 때문이다.** 표시 세트 4종(§4.1)에도 없어서, 피처가 아니면 수집만 하고 어디에도 쓰이지 않는다. HRV와 **같이 결측되므로**(둘 다 워치 착용 여부에 의존) 결측 분기가 새로 생기지도 않는다 — 이미 `HRV` 때문에 존재하던 문제의 범위가 같을 뿐이다.

**각성 총 시간 — 배정 여전히 미정** (2026-08-06 추가). 수집은 확정이나 **어느 지표에 붙일지는 B1에서 함께 정한다.** 배정 전까지 스코어링에 넣지 않는다. 붙이면 8번째 피처가 된다.

| 수면 피처 | 후보 지표 | 근거 |
|---|---|---|
| 각성 총 시간 | `DARK_CIRCLE` | 각성 횟수와 같은 계열. 다만 **횟수와 중복 상관** 우려가 그대로 따라온다 |

**제외 유지** — 저장은 하지만 피처가 아니다:

| 항목 | 이유 |
|---|---|
| 코어 수면 | `TOTAL_SLEEP`과 강하게 겹친다. 얕은 수면이라 피부 회복과의 직접 연결도 약하다 |
| 기상 시각 | 취침 규칙성은 `sleep_onset_time`으로만 계산한다(§4.1). 기상 시각은 총 수면 시간 검증·화면 표현용 |

논의가 필요한 지점:
- `COMPLEXION`에 3개가 몰려 있다. **그중 2개(HRV·안정시 심박)가 같은 신호원**이라 중복 상관이 있다
- `DARK_CIRCLE`에 야간 각성과 총 수면 시간이 둘 다 붙어 있어 상관이 중복 계산될 수 있다. **각성 총 시간까지 붙이면 3개가 몰린다**
- **횟수와 총 시간이 같은 5분 임계값을 쓰므로**(§4.1) 둘은 같은 구간 집합에서 나온다. 중복 상관이 임계값 확정 전보다 강해졌다 — 둘 다 `DARK_CIRCLE`에 붙이는 안은 이 점을 감안해 판단한다
- **`DEEP_SLEEP`과 `REM_SLEEP`도 서로 상관이 높다** — 총 수면 시간이 늘면 함께 는다. 승격의 대가로 받아들인 부분이며, B1에서 가중합 정규화 시 고려한다
- 지표당 피처 개수가 다르면 가중합 정규화 방식도 지표별로 달라져야 한다
- 피처 7개는 `personal_weight` 7행을 뜻한다. **학습에 필요한 검증 횟수가 5행일 때보다 늘어난다** — HOME-08 신뢰도 등급 구간(§7 L8)을 이 숫자에 맞춰 정한다

### 9.2 그 밖의 임시값

| 항목 | 현재 상태 | 확정 시점 |
|---|---|---|
| 가중합 스코어링 공식·초기 가중치 | 미정 (§7 **B1**) | HOME-03 구현 직전 |
| ~~등급 컷오프 구간~~ | **확정** → §10.1 | 2026-08-06 |
| ~~판정 오차 구간~~ | **확정** → §10.2 | 2026-08-06 |
| 상관 강도 라벨 4단계 계수 구간 | 미정 (§7 L7) | REP-07 구현 직전 |
| 모델 신뢰도 등급 일수 구간 | 미정 (§7 L8) | MY-01 구현 직전 |
| 트리아지 발동 임계값 | 미정 (§7 L6) | REP-09 구현 직전 |
| 수면 목표값 (깊은수면 분·취침 시각) | 미정 (§7 **B6**) | REP-02 구현 직전 |

### 9.3 구현 시 지킬 것

임시값이 코드 곳곳에 흩어지면 재확정 때 전부 찾아다녀야 한다. **한 곳에 모은다.**

```java
// domain/skin/ScoringPolicy.java — 스코어링 파라미터를 모으는 곳
public final class ScoringPolicy {
    // ⚠️ 임시값 — PRD §9.1. 팀 논의로 재확정 예정
    public static final Map<SleepFeature, SkinMetric> FEATURE_METRIC_MAP = ...;

    // 확정값 — PRD §10.1 (등급 컷오프, 세 지표 공통)
    public static final int[] GRADE_CUTOFFS = { 25, 50, 75 };
    // 확정값 — PRD §10.2 (판정 오차 구간, 절댓값 기준)
    public static final int VERDICT_HIT_MAX = 5;
    public static final int VERDICT_CLOSE_MAX = 15;
    ...
}
```

임시값을 참조하는 코드에는 주석으로 출처를 남긴다: `// 임시값 (PRD §9.1)`

---

## 10. 확정된 정책값

§9의 임시값과 달리 **팀 논의로 확정된 값**이다. `ScoringPolicy` 상수로 두는 것은 같지만 재확정 대기 상태가 아니다.

### 10.1 등급 컷오프 (B2 — 2026-08-06 확정)

0~100 예보 점수를 등급 라벨로 매핑한다. **세 지표가 같은 구간을 쓴다.**

| 지수 | 등급 | 의미 |
|---|---|---|
| 0~25 | **위험** | 피부 컨디션 저하 가능성이 높음 |
| 26~50 | **주의** | 피부 컨디션 저하 가능성 있음 |
| 51~75 | **보통** | 일반적인 수준 |
| 76~100 | **안정** | 수면으로 인한 피부 영향이 낮음 |

**25점씩 4등분이라 경계값이 `25`·`50`·`75` 세 개뿐이다.** 지표별로 다른 구간을 두지 않으므로 상수도 세 개면 된다.

> ⚠️ **원본 팀 협의 자료(2026-08-06)는 의미 열이 등급과 역순으로 적혀 있었다** — `0~25`(위험)에 "수면으로 인한 피부 영향이 낮음"이 붙는 식이었다. 자료 상단의 "숫자가 클수록 좋은 것"과 등급 라벨 방향이 서로 일치하므로 **그 둘을 기준으로 삼고 의미 열을 바로잡아 옮겼다.** 위 표가 확정본이다.
> 이 방향이 뒤집히면 값 범위는 정상이라 어떤 검증에도 안 걸리고 **화면 문구만 정반대로 나간다.** `DARK_CIRCLE`과 같은 종류의 함정이다(§4.1).

### 10.2 판정 오차 구간 (B3 — 2026-08-06 확정)

HOME-07 검증에서 `예보 − 실측`을 판정 라벨로 매핑한다. **지표별로 따로 판정하고, 적중률은 3개 중 `적중` 비율이다.**

| 예보 − 실측 | 판정 | 의미 |
|---|---|---|
| ±5점 이내 | **적중** | 예보와 실측이 거의 일치 |
| ±6~15점 | **근접** | 비슷하게 예측 |
| −16점 이하 | **과소예측** | 예보 점수를 실제보다 낮게 냈다 |
| +16점 이상 | **과대예측** | 예보 점수를 실제보다 높게 냈다 |

**"과소/과대"는 점수 축 기준이다.** 점수는 높을수록 좋으므로 **점수를 과소예측한 것은 피부 위험을 과대평가한 것**이다. 두 축이 서로 반대라, 문구를 쓸 때 어느 축인지 밝히지 않으면 뜻이 뒤집힌다 — "위험을 낮게 예측했다"는 `과대예측`을 가리킨다.

**`적중`·`근접`은 절댓값으로, `과소`·`과대`는 부호로 가른다.** `|차이| ≤ 5` → `|차이| ≤ 15`를 먼저 걸고 남은 것만 부호를 본다. 순서를 바꾸면 −5점이 `과소예측`으로 새어 나간다.

---

## 11. 참고

- Notion 기능명세서: https://app.notion.com/p/3aff18125428805fa7c2ec191eb58387
- 상위 페이지: 멋사 14기 중앙해커톤 > 기능명세서 작성
- 현재 구현 상태: `GET /api/v1/health` 헬스체크만 존재
- 관련 문서: [architecture.md](architecture.md) · [erd.md](erd.md) · [conventions.md](conventions.md) · [workflow.md](workflow.md)
